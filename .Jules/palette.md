## 2026-05-04 - Added aria-labels to external links
**Learning:** Found several external social links (GitHub, LinkedIn) and demo links represented only by icons, rendering them unclear for screen reader users. Adding `aria-label` attributes significantly boosts accessibility.
**Action:** Always include `aria-label` on links that use icons instead of text to convey their purpose.
